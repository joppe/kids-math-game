/*
describe('Array checking', function () {
    describe('foreach', function () {
        it('should handle an array', function () {
            var s = '';

            Aap.Array.forEach([1, 2, 3, 4], function () {
                s += '.';
            });
            expect(s).toBe('....');
        });

        it('should not handle non arrays', function () {
            var s = '';

            Aap.Array.forEach(null, function () {
                s += '.';
            });

            expect(s).toBe('');
        });
    });

    describe('indexOf', function () {
        var a = [1, 2, 3, 4, 1 ,2, 3, 4];

        it('should find a value', function () {
            expect(Aap.Array.indexOf(a, 1)).toBe(0);
        });

        it('should not find a value', function () {
            expect(Aap.Array.indexOf(a, 9)).toBe(-1);
        });

        it('should not find a value', function () {
            expect(Aap.Array.indexOf(a, 1, 2)).toBe(4);
        });
    });
});*/
