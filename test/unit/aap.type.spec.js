/*
describe('Type checking', function () {
    describe('isArray', function () {
        it('to return true on an empty array', function () {
            expect(Aap.Type.isArray([])).toBe(true);
        });

        it('to return true on an non empty array', function () {
            expect(Aap.Type.isArray([1, 2, 3, 4])).toBe(true);
        });

        it('to return false on a null value', function () {
            expect(Aap.Type.isArray(null)).toBe(false);
        });

        it('to return false on a undefined value', function () {
            expect(Aap.Type.isArray(undefined)).toBe(false);
        });

        it('to return false on a integer value', function () {
            expect(Aap.Type.isArray(1)).toBe(false);
        });

        it('to return false on a float value', function () {
            expect(Aap.Type.isArray(1.2)).toBe(false);
        });

        it('to return false on a boolean value', function () {
            expect(Aap.Type.isArray(true)).toBe(false);
        });

        it('to return false on an object value', function () {
            expect(Aap.Type.isArray({foo: []})).toBe(false);
        });

        it('to return false on a function value', function () {
            expect(Aap.Type.isArray(function () {return [];})).toBe(false);
        });
    });

    describe('isNumber', function () {
        it('to return true on a positive integer', function () {
            expect(Aap.Type.isNumber(3)).toBe(true);
        });

        it('to return true on a negative integer', function () {
            expect(Aap.Type.isNumber(-103)).toBe(true);
        });

        it('to return true on 0', function () {
            expect(Aap.Type.isNumber(0)).toBe(true);
        });

        it('to return true on a float', function () {
            expect(Aap.Type.isNumber(1.23)).toBe(true);
        });

        it('to return false on a string', function () {
            expect(Aap.Type.isNumber('1.23')).toBe(false);
        });
    });

    describe('isInt', function () {
        it('to return true on a positive integer', function () {
            expect(Aap.Type.isInt(4)).toBe(true);
        });

        it('to return true on a negative integer', function () {
            expect(Aap.Type.isInt(-2344)).toBe(true);
        });

        it('to return true on 0', function () {
            expect(Aap.Type.isInt(0)).toBe(true);
        });

        it('to return false on a float', function () {
            expect(Aap.Type.isInt(3.14)).toBe(false);
        });
    });
});*/
